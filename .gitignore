__pycache__/
*.pyc
.env
# Runtime state: uploads, dataset databases, job files, chat sessions and
# data/.session_secret, which signs the login cookie. Never commit any of it.
# Written as data/* rather than data/ so the negation below can take effect -
# git does not descend into an excluded directory.
data/*
!data/samples/
.venv/
venv/
.DS_Store

# Build artifact for File Manager uploads
esp-deploy.zip
# Built by scripts/fetch_asn.py — 27 MB, regenerate rather than commit
data/asn.db
